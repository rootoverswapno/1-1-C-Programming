#include<stdio.h>
int main()
{
 int n,t,i,j,l;
    char *p;
    printf("Enter number:");
    scanf("%d",&n);
    scanf("%d",&t);
    p=(char*)malloc(n*sizeof(char));
    gets(p);
    for(i=0;p[i]!='\0';i++)
    {
        l++;
    }
    for(j=0;l-1>=0;l--)
    {

    }



    return 0;
}
