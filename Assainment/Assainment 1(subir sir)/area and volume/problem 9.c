#include<stdio.h>
int main()
{
 int a,area,volume;
 printf("Enter a side of cube :");
 scanf("%d",&a);
 area=6*a*a;
 volume=a*a*a;
 printf("area is %d",area);
 printf("\nvolume is %d",volume);



    return 0;
}
